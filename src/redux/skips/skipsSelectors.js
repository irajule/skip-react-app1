export const selectSkips = (state) => state.skips.skips;
export const selectSelectedSkip = (state) => state.skips.selected;
export const selectLoading = (state) => state.skips.loading;
export const selectError = (state) => state.skips.error;
